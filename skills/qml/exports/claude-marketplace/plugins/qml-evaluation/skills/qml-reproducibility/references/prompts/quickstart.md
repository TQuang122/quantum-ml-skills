# qml-reproducibility quickstart prompts

## Prompt 1 — make this QML run reproducible

Use `qml-reproducibility` to make this PennyLane + PyTorch QML experiment rerunnable. Define the seed policy, split discipline, config capture, backend/shot recording, and artifact naming so the run can be reproduced later.

## Prompt 2 — prepare reproducible benchmark inputs

Before benchmarking these QML branches, use `qml-reproducibility` to lock down the split, config, backend metadata, and run manifest so the comparison is defensible.
