# qml-debugging quickstart prompts

## Prompt 1 — diagnose a broken QML workflow

Use `qml-debugging` to diagnose this PennyLane + PyTorch QML issue. Identify the failing stage, collect concrete evidence, classify the failure, and recommend the next owner skill instead of applying multiple speculative fixes.

## Prompt 2 — find the first blocking failure

This QML workflow is failing, but it is unclear whether the problem is the model, the PyTorch boundary, the training loop, runtime/performance confusion, or backend behavior. Use `qml-debugging` to find the first blocking failure and route the fix correctly.
