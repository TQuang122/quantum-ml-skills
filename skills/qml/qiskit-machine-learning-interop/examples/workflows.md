# qiskit-machine-learning-interop example workflows

## Example 1 — evaluate `TorchConnector`

- Input: existing PennyLane + PyTorch classifier plus curiosity about Qiskit ML
- Skill outcome: creates a narrow comparison branch instead of replacing the main architecture

## Example 2 — reject unnecessary interop

- Input: request to “use Qiskit everywhere” with no specific feature need
- Skill outcome: recommends staying on plugin-backed PennyLane + Qiskit execution instead
