# qml-pytorch-performance-patterns quickstart prompts

## Prompt 1 — optimize a PennyLane + PyTorch workload

Profile this PennyLane + PyTorch workload, identify whether batching, preprocessing, simulator calls, or device placement is the main bottleneck, then improve performance without changing the task definition.

## Prompt 2 — audit a claimed speedup

Review this performance branch and determine whether the reported speedup is real, measured fairly, and numerically consistent with the reference implementation.
